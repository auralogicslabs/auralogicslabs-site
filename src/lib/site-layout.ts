/** Shared site content width, use `max-w-site` or `siteContainerClass` everywhere. */
export const SITE_MAX_WIDTH_PX = 1600;

export const siteContainerClass =
  "w-full max-w-site mx-auto px-6 sm:px-10 lg:px-16";
